  <%= form_for(:user, :url => {:controller => 'users', :action => 'create'}) do |f| %>
  <%= form_with scope: :user, url: users_path, local: true do |form| %>
<%= form_for :user, @user, :url => { :action => "index" } do |f| %>

<%= form_for(:user, :url =>  users_path), method: :get do |f| %>
<%= form_for @comment, url: article_comment_path(@article, @comment), method: :put do |form| %> 
<%= form_with scope: :user, url: users_path, local: true do |form| %>


<!DOCTYPE html>
<html>
<head>
  <title></title>
</head>
<body>
<center>
   <h1> Welcome to dashboard </h1>
 </center>
<p style="text-align: right; font-size: 20px;">
  <%= link_to 'Logout', users_path %>
<table>
  <tr>
    <h3><th>First Name</th></h3>
    <th><br></th>
    <th><br></th>
    <h3><th>Last Name</th></h3>
    <th><br></th>
    <th><br></th>
    <h3><th>Email</th></h3>
    <th><br></th>
    <th><br></th>
    <h3><th>Password</th></h3>
    <th colspan="3"></th>
  </tr>
 
  <% @users.each do |user| %>
    <tr>
      <td><%= user.first_name %></td>
      <td><br></td>
      <td><br></td>
      <td><%= user.last_name %></td>
      <td><br></td>
      <td><br></td>
      <td><%= user.email %></td>
      <td><br></td>
      <td><br></td>
      <td><%= user.encrypted_password %></td>

      <td><%= link_to 'Show', article_path(user) %></td>
      <td><br></td>
      <td><br></td>
      <td><%= link_to 'Edit', edit_user_path(user) %></td>
      <td><br></td>
      <td><br></td>
      <td><%= link_to 'Destroy', user_path(user),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
    </tr>
    <% end %>
</table>
</body>
</html>


<%= form_for @user, url: users_path(@user), method: :put do |form| %>


<%= text_field_tag :email%>
<%= text_field_tag :encrypted_password  %>